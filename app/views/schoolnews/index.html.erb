<% provide(:title, 'Home Page '+@page_num.to_s) %>
<% _idx = @page_num * 8 %>

<div class="row">
  <% @news_list.each do |news|%>
    <div class="news">
      <p class="news-title">
        <%= _idx += 1 %>.<%= link_to news.title, schoolnews_path(news) %><%= safe_source(news) %>
      </p>
      <p class="news-info">
        by <%= news.author %> <%= cal_time_before(news.created_at)+' ago' %> | <%= pluralize(safe_comments_counts(news), 'comment') %>
      </p>
    </div>
  <% end %>
</div>

<%= link_to 'More', '/news?p='+(@page_num + 1).to_s %>